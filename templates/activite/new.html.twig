{% extends 'base.html.twig' %}

{% block title %}Nouvelle Activité{% endblock %}

{% block body %}
<div class="card mb-4">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0"><i class="bi bi-plus-circle me-2"></i> Créer une nouvelle activité</h5>
        <a href="{{ path('app_activite_index') }}" class="btn btn-ghost">
            <i class="bi bi-arrow-left"></i> Retour à la liste
        </a>
    </div>
    <div class="card-body">
        {{ include('activite/_form.html.twig', {'button_label': 'Ajouter'}) }}
    </div>
</div>
{% endblock %}


