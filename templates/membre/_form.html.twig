{{ form_start(form, {'attr': {'class': 'row g-3'}}) }}

    {# --- Ligne 1 : Identité --- #}
    <div class="col-md-6">
        {{ form_row(form.nom, {
            label: 'Nom',
            attr: {'class': 'form-control', 'placeholder': 'Ex : Dupont'}
        }) }}
    </div>

    <div class="col-md-6">
        {{ form_row(form.prenom, {
            label: 'Prénom',
            attr: {'class': 'form-control', 'placeholder': 'Ex : Charles'}
        }) }}
    </div>

    {# --- Ligne 2 : Contact & Connexion --- #}
    <div class="col-md-6">
        {{ form_row(form.login, {
            label: 'Identifiant',
            attr: {'class': 'form-control', 'placeholder': 'Nom d’utilisateur'}
        }) }}
    </div>

    <div class="col-md-6">
        {{ form_row(form.email, {
            label: 'Email',
            attr: {'class': 'form-control', 'placeholder': 'exemple@mail.com'}
        }) }}
    </div>

    {# --- Ligne 3 : Sécurité & Rôle --- #}
    <div class="col-md-6">
        {# 👇 C'est le nouveau champ Mot de passe #}
        {{ form_row(form.plainPassword, {
            label: 'Mot de passe',
            help: 'Laisser vide si vous ne voulez pas le modifier',
            attr: {'class': 'form-control', 'placeholder': '********', 'autocomplete': 'new-password'}
        }) }}
    </div>

    <div class="col-md-6">
        {{ form_row(form.role, {
            label: 'Rôle',
            attr: {'class': 'form-select'}
        }) }}
    </div>

    {# --- Actions --- #}
    <div class="d-flex justify-content-end gap-2 mt-4">
        <a href="{{ path('app_membre_index') }}" class="btn btn-ghost">
            <i class="bi bi-x-circle"></i> Annuler
        </a>
        <button class="btn btn-primary">
            <i class="bi bi-check-circle"></i> {{ button_label|default('Sauvegarder') }}
        </button>
    </div>

{{ form_end(form) }}