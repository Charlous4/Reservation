{% extends 'base.html.twig' %}

{% block title %}Nouvelle Session{% endblock %}

{% block body %}
<div class="card mb-4">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0"><i class="bi bi-plus-circle me-2"></i> Créer une nouvelle session</h5>
        <a href="{{ path('app_session_index') }}" class="btn btn-ghost">
            <i class="bi bi-arrow-left"></i> Retour à la liste
        </a>
    </div>
    <div class="card-body">
        {{ include('session/_form.html.twig', {'button_label': 'Ajouter'}) }}
    </div>
</div>
{% endblock %}
