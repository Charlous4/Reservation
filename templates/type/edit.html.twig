{% extends 'base.html.twig' %}

{% block title %}Modifier Type{% endblock %}

{% block body %}
<div class="card mb-4">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0"><i class="bi bi-pencil-square me-2"></i> Modifier le type #{{ type.id }}</h5>
        <a href="{{ path('app_type_index') }}" class="btn btn-ghost">
            <i class="bi bi-arrow-left"></i> Retour
        </a>
    </div>
    <div class="card-body">
        {{ include('type/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>
    <div class="card-footer d-flex justify-content-end gap-2">
        {{ include('type/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
